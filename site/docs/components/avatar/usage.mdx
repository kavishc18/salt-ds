---
# Leave the frontmatter as is
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  label: Usage
data:
  $ref: ./#/data
---

### Import

{/* Update the text and code snippet below as needed: */}

To import Avatar from the core Salt package, use:

```
import { avatar} from "@salt-ds/core";
```

### Props

{/* Update packageName and componentName below as needed */}
{/* packageName is optional and defaults to "core" if omitted */}

<PropsTable packageName="core" componentName="avatar" />

### Content

When using Avatar we recommend you add text describing what the image will represent:

- `name="Person's name"` - when the image represents a person
- `name="Group's name"` - when the image represents a group
- `name="Entity's name"` - when the image represents an entity
- `name="Channel's name"` - when the image represents a communication channel
- `name=""` - when the name is provided next to the Avatar

{/* ### References

Add text from "References" section here when available, or else remove this
section. */}
