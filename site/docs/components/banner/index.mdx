---
title: Component Name
data:
  description: Banner is used in two ways: to let users know about important information, and to provide feedback about an error, warning or an information update.

  # Fill in the info from the content template's "Metadata" table below.
  # To omit optional items, comment them out with #
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/packages/core/src/banner/Banner.tsx"
  package:
    name: "@salt-ds/core"
  # alsoKnownAs: ["Other Name", "Another Name"]
  relatedComponents: [
      # Add a { name: "...", relationship: "..." } block for each
      # related component here (separated by commas).
      # Permitted values for relationship are: "similarTo" or
      # "contains".
      { name: "BannerActions", relationship: "contains" },
      { name: "BannerContent", relationship: "contains" },
    ]
  # stickerSheet: "https://figma.com/..."

# Leave this as is
layout: DetailComponent
---

{/* This area stays blank */}
